{
  "homepage": "https://davorj94.github.io/to-do-app-improved",
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "predeploy": "snowpack build",
    "deploy": "gh-pages -d build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "snowpack": "^3.0.1"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
